steps:
  - name: "gcr.io/cloud-builders/docker"
    dir: "backend"
    args:
      - "build"
      - "-t"
      - "gcr.io/$PROJECT_ID/factorybrain-edge-backend:$COMMIT_SHA"
      - "-f"
      - "Dockerfile"
      - "."

  - name: "gcr.io/cloud-builders/docker"
    args:
      - "push"
      - "gcr.io/$PROJECT_ID/factorybrain-edge-backend:$COMMIT_SHA"

images:
  - "gcr.io/$PROJECT_ID/factorybrain-edge-backend:$COMMIT_SHA"

options:
  logging: CLOUD_LOGGING_ONLY